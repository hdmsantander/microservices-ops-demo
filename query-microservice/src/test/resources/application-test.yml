# Use HTTP for Zipkin in tests (Kafka client disabled; no Kafka required)
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
  data:
    redis:
      repositories:
        enabled: false
  task:
    scheduling:
      enabled: false
  boot:
    admin:
      client:
        enabled: false

management:
  tracing:
    export:
      zipkin:
        transport: http
        endpoint: http://localhost:9411/api/v2/spans
